# Organic Store — Customer Frontend environment variables
#
# Base URL of the Laravel REST API (without the /api/v1 suffix is fine;
# this string is used as the axios baseURL verbatim).
# In development, an empty value lets Vite proxy /api to the Laravel server.
VITE_API_BASE_URL=/api/v1